#include <stdio.h>

/**
 * main - I sometimes suffer from insomnia. And when I can't
 * fall asleep, I play what I call the alphabet game
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	/*declaration of variable*/
	char a;

	/*condition to print small letter*/
	for (a = 'a'; a <= 'z'; a++)
	{
		putchar (a);
	}

		putchar ('\n');
		return (0);
}
