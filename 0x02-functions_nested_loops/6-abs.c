#include "main.h"

/**
  *_abs - This function computes the absolute value of an integer
  *@c: this is the return value for the program
  *Return: This function return an integer
  */

int _abs(int c)
{
	/*Declare the variable*/
	/*check if value is less than or equal to -1*/
	/*Multiply the value by -1*/
	/* then return the integer*/


	if (c <= -1)
	{
		c *= -1;
	}
	return (c);
}
